package characters.classes;

import characters.Character;

public class Warrior  extends Character {
    public Warrior(String name, int level, int primaryAttributes, int totalAttributes) {
        super(name, level, primaryAttributes, totalAttributes);
    }

    @Override
    public void levelUp() {
        super.levelUp();
    }
}
