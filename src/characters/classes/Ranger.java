package characters.classes;

import characters.Character;

public class Ranger extends Character {
    public Ranger(String name, int level, int primaryAttributes, int totalAttributes) {
        super(name, level, primaryAttributes, totalAttributes);
    }

    @Override
    public void levelUp() {
        super.levelUp();
    }
}
