package characters.classes;

import characters.Character;

public class Mage extends Character {
    public Mage(String name, int level, int primaryAttributes, int totalAttributes) {
        super(name, level, primaryAttributes, totalAttributes);
    }

}
